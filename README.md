# Medium automation test

Small test fixture for checking Markdown-to-HTML draft formatting. It has no
publishing automation and does not authorize actions on Medium.

- `post.md` is the tracked source fixture.
- `post.html` is generated local output and is intentionally ignored.

To inspect the fixture, render it with a local Markdown tool of your choice and
compare the output without uploading or importing it anywhere.
